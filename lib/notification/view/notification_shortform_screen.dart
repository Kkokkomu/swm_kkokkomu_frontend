import 'package:flutter/material.dart';

class NotificationShortFormScreen extends StatelessWidget {
  static String get routeName => 'notification-shortform';

  const NotificationShortFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
